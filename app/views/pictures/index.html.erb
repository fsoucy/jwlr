<% @pictures.each do |picture| %>
<div class="picture">
  <%= image_tag picture.photo.url %>
  <%= image_tag picture.photo_cropped.url %>
  <%= link_to "Delete", picture, method: :delete,
      data: { confirm: "Are you sure?" } %>
  <%= link_to "Edit", edit_picture_url(picture) %>
</div>
<% end %>
