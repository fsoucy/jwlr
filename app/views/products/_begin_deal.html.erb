<%= form_for(current_user.active_deals.build) do |f| %>
  <div><%= hidden_field_tag :seller_id, @product.user.id %>
    <%= hidden_field_tag :product_id, @product.id %></div>
  <%= f.submit "Negotiate Deal", class: "btn btn-primary" %>
<% end %>
