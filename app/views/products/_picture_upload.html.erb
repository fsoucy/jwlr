<div class="dropper" id="dropper">
  <%= form_for @picture, url: product_path(@product) + "/pictures", html: { multipart: true, class: "dropzone"} do |f|  %>
  <% end %>
  <a href="<%= product_url(@product.id) %>"><button class="pure-button pure-button-primary submit_the_form submit_product">Done</button></a>
</div>
